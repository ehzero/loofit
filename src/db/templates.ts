// Kept as a compatibility export for database-layer imports outside the app.
// The product template definition lives with the selection UI configuration.
export { ROUTINE_TEMPLATES } from '@/src/config/routine-templates';
